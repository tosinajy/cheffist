<!doctype html>
<html lang="en" class="scroll-smooth">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{{ title or siteName or "Cheffist" }}</title>
    {% if canonical %}<link rel="canonical" href="{{ canonical }}" />{% endif %}
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,500;9..144,700&family=Plus+Jakarta+Sans:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
      tailwind.config = {
        theme: {
          extend: {
            fontFamily: {
              sans: ["Plus Jakarta Sans", "ui-sans-serif", "system-ui"],
              display: ["Fraunces", "ui-serif", "Georgia"]
            },
            colors: {
              ink: "#0d1b2a",
              mist: "#e0e7ff",
              frost: "#eef3ff",
              ember: "#e67e22",
              pine: "#1f6f78"
            },
            boxShadow: {
              panel: "0 18px 40px -24px rgba(13, 27, 42, 0.45)"
            }
          }
        }
      };
    </script>
    <link rel="stylesheet" href="/assets/styles.css" />
  </head>
  <body class="min-h-screen bg-[#f4f7fb] font-sans text-ink antialiased">
    <div class="pointer-events-none fixed inset-0 -z-10 overflow-hidden">
      <div class="absolute -left-28 -top-28 h-80 w-80 rounded-full bg-[#d6e5ff] blur-3xl opacity-70"></div>
      <div class="absolute right-[-6rem] top-1/3 h-96 w-96 rounded-full bg-[#cdece5] blur-3xl opacity-70"></div>
      <div class="absolute bottom-[-7rem] left-1/3 h-80 w-80 rounded-full bg-[#ffe2c2] blur-3xl opacity-70"></div>
    </div>

    <header class="mx-auto max-w-7xl px-4 pb-4 pt-6 sm:px-6 lg:px-8">
      <div class="rounded-2xl border border-white/80 bg-white/80 px-5 py-4 shadow-panel backdrop-blur">
        <div class="flex flex-wrap items-center justify-between gap-4">
          <a href="/" class="inline-flex items-center gap-3">
            <span class="inline-flex h-10 w-10 items-center justify-center rounded-xl bg-ink text-white">C</span>
            <span>
              <strong class="block font-display text-xl leading-none">Cheffist</strong>
              <small class="text-xs text-slate-500">Shelf-life intelligence</small>
            </span>
          </a>
          <nav class="flex flex-wrap items-center gap-2 text-sm font-medium">
            <a class="rounded-full px-4 py-2 text-slate-600 transition hover:bg-frost hover:text-ink" href="/">Foods</a>
            <a class="rounded-full px-4 py-2 text-slate-600 transition hover:bg-frost hover:text-ink" href="/food-left-out-calculator/">Calculator</a>
            <a class="rounded-full px-4 py-2 text-slate-600 transition hover:bg-frost hover:text-ink" href="/disclaimer/">Disclaimer</a>
          </nav>
        </div>
      </div>
    </header>

    <main class="mx-auto max-w-7xl px-4 pb-16 sm:px-6 lg:px-8">
      {{ content | safe }}
    </main>

    <footer class="mx-auto max-w-7xl px-4 pb-10 sm:px-6 lg:px-8">
      <div class="rounded-2xl border border-white/80 bg-white/80 px-5 py-4 text-sm text-slate-600 shadow-panel backdrop-blur">
        Built for practical food safety decisions. Educational only, not medical advice.
      </div>
    </footer>
  </body>
</html>
