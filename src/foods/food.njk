<article class="space-y-8">
  {% set adsEnabled = (adContext and adContext.enabled) or (adConfig and adConfig.enableAds) %}

  <header class="space-y-3">
    <p class="text-xs uppercase tracking-[0.2em] text-muted">Food Storage Guide</p>
    <h1 class="text-3xl font-semibold tracking-tight sm:text-4xl">How long does {{ food.name }} last?</h1>
    <p class="max-w-3xl text-sm text-muted sm:text-base">
      Conservative storage windows for pantry, fridge, and freezer plus spoilage checks and practical handling tips.
    </p>
  </header>

  {% set cardVariant = "elevated" %}
  {% set cardClassName = "space-y-4" %}
  {% set cardContent %}
  <section aria-label="Storage Summary" class="space-y-4">
    <h2>Storage Summary</h2>
    <div class="grid grid-cols-1 gap-3 sm:grid-cols-3">
      <div class="rounded-xl border border-border bg-surfaceElevated p-4">
        <p class="text-xl">[ ]</p>
        <p class="text-xs uppercase tracking-wide text-muted">Pantry</p>
        <p class="mt-1 text-lg font-semibold tracking-tight">
          {{ food.pantry_min_days }}-{{ food.pantry_max_days }} days
        </p>
      </div>
      <div class="rounded-xl border border-border bg-surfaceElevated p-4">
        <p class="text-xl">[ ]</p>
        <p class="text-xs uppercase tracking-wide text-muted">Fridge</p>
        <p class="mt-1 text-lg font-semibold tracking-tight">
          {{ food.fridge_min_days }}-{{ food.fridge_max_days }} days
        </p>
      </div>
      <div class="rounded-xl border border-border bg-surfaceElevated p-4">
        <p class="text-xl">[ ]</p>
        <p class="text-xs uppercase tracking-wide text-muted">Freezer</p>
        <p class="mt-1 text-lg font-semibold tracking-tight">
          {{ food.freezer_min_days }}-{{ food.freezer_max_days }} days
        </p>
      </div>
    </div>
  </section>
  {% endset %}
  {% include "components/ui-card.njk" %}

  {% if adsEnabled %}
  {% set adSlot = {
    enabled: true,
    zone: "food_after_hero"
  } %}
  {% include "components/ad-slot.njk" %}
  {% endif %}

  {% set cardVariant = "default" %}
  {% set cardContent %}
  <section>
    <h2>Spoilage signs</h2>
    <ul class="space-y-3">
      {% for sign in food.spoilage_signs %}
      <li class="flex items-start gap-3">
        <span class="mt-2 h-2 w-2 rounded-full bg-rose-400/80" aria-hidden="true"></span>
        <span>{{ sign }}</span>
      </li>
      {% endfor %}
    </ul>
  </section>
  {% endset %}
  {% include "components/ui-card.njk" %}

  {% if adsEnabled %}
  {% set adSlot = {
    enabled: true,
    zone: "food_after_spoilage"
  } %}
  {% include "components/ad-slot.njk" %}
  {% endif %}

  {% set cardVariant = "glass" %}
  {% set cardContent %}
  <section>
    <h2>Storage tips</h2>
    <ul class="space-y-2 text-sm text-muted">
      {% for tip in food.storage_tips %}
      <li>{{ tip }}</li>
      {% endfor %}
    </ul>
  </section>
  {% endset %}
  {% include "components/ui-card.njk" %}

  {% if food.notes %}
  {% set cardVariant = "default" %}
  {% set cardContent %}
  <section>
    <h2>Notes</h2>
    <p>{{ food.notes }}</p>
  </section>
  {% endset %}
  {% include "components/ui-card.njk" %}
  {% endif %}

  {% set cardVariant = "elevated" %}
  {% set cardContent %}
  <section class="space-y-4">
    <h2>Related tools</h2>
    <div class="grid grid-cols-1 gap-4 md:grid-cols-3">
      <article class="rounded-xl border border-border bg-surfaceElevated p-4">
        <h3 class="text-base font-semibold">Sit-Out Risk</h3>
        <p class="mt-2 text-sm text-muted">
          Estimate conservative room-temperature risk with time and temperature.
        </p>
        <p class="mt-4">
          <a
            href="{{ calculatorFoodLink }}"
            class="inline-flex items-center justify-center rounded-xl bg-white/10 px-4 py-2 text-sm font-semibold text-text no-underline transition hover:bg-white/15"
          >
            Open Sit-Out Tool
          </a>
        </p>
      </article>

      <article class="rounded-xl border border-border bg-surfaceElevated p-4">
        <h3 class="text-base font-semibold">Power Outage</h3>
        <p class="mt-2 text-sm text-muted">
          Evaluate outage windows with fridge/freezer conditions and conservative assumptions.
        </p>
        <p class="mt-4">
          <a
            href="/power-outage-calculator/"
            class="inline-flex items-center justify-center rounded-xl bg-white/10 px-4 py-2 text-sm font-semibold text-text no-underline transition hover:bg-white/15"
          >
            Open Outage Tool
          </a>
        </p>
      </article>

      <article class="rounded-xl border border-border bg-surfaceElevated p-4">
        <h3 class="text-base font-semibold">Freezer Recovery</h3>
        <p class="mt-2 text-sm text-muted">
          Check thaw/refreeze scenarios with caution-first recommendations.
        </p>
        <p class="mt-4">
          <a
            href="{{ freezerRecoveryLink }}"
            class="inline-flex items-center justify-center rounded-xl bg-white/10 px-4 py-2 text-sm font-semibold text-text no-underline transition hover:bg-white/15"
          >
            Open Recovery Tool
          </a>
        </p>
      </article>
    </div>
  </section>
  {% endset %}
  {% include "components/ui-card.njk" %}

  {% set cardVariant = "default" %}
  {% set cardContent %}
  <section aria-label="Safety Disclaimer">
    <h2>Safety disclaimer</h2>
    <p>
      Educational only. This content is not medical advice. When in doubt, throw it out.
      Read our <a href="/disclaimer/">full disclaimer</a>.
    </p>
  </section>
  {% endset %}
  {% include "components/ui-card.njk" %}

  {% set cardVariant = "default" %}
  {% set cardContent %}
  <section>
    <h2>Sources used</h2>
    <ul class="space-y-2 text-sm">
      {% for source in sourcesUsed %}
      <li class="text-muted">
        <a href="{{ source.url }}">{{ source.title }}</a>
        <span class="text-xs">({{ source.publisher }})</span>
      </li>
      {% endfor %}
    </ul>
  </section>
  {% endset %}
  {% include "components/ui-card.njk" %}

  {% set cardVariant = "default" %}
  {% set cardContent %}
  <section>
    <h2>Dataset metadata</h2>
    <p class="text-sm text-muted">Version {{ dataset.version }} | Last updated {{ dataset.last_updated }}</p>
  </section>
  {% endset %}
  {% include "components/ui-card.njk" %}

  {% set cardVariant = "default" %}
  {% set cardContent %}
  <section>
    <h2>Left out safety</h2>
    <p>
      Check conservative sit-out guidance for {{ food.name }} with the calculator and scenario pages.
      This guidance is educational only and not medical advice.
    </p>
    <p>
      <a href="{{ calculatorFoodLink }}">Open calculator for {{ food.name }}</a>
    </p>
    {% if topSitoutPages.length %}
    <ul class="list-disc space-y-1 pl-5">
      {% for scenario in topSitoutPages %}
      <li>
        <a href="{{ scenario.url }}">
          {{ food.name }}: {{ scenario.label }}
        </a>
        {% if not scenario.isStaticPrefill %}
        (calculator prefill)
        {% endif %}
      </li>
      {% endfor %}
    </ul>
    {% else %}
    <p>Scenario pages are not available yet for this food.</p>
    {% endif %}
  </section>
  {% endset %}
  {% include "components/ui-card.njk" %}

  {% set cardVariant = "default" %}
  {% set cardContent %}
  <section>
    <h2>Most common scenarios</h2>
    <ul class="list-disc space-y-1 pl-5">
      <li>Room temperature and elapsed time together drive risk. Hotter rooms shorten conservative limits.</li>
      <li>Covered food may still become unsafe over time; use smell, texture, and visible spoilage signs as extra checks.</li>
      <li>Higher-risk foods and higher-risk households should use stricter cutoffs and discard sooner when uncertain.</li>
      <li>If conditions are unclear, follow the disclaimer guidance: when in doubt, throw it out.</li>
    </ul>
  </section>
  {% endset %}
  {% include "components/ui-card.njk" %}

  {% set cardVariant = "default" %}
  {% set cardContent %}
  <section>
    <h2>Related foods</h2>
    {% if relatedFoods.length %}
    <ul class="list-disc space-y-1 pl-5">
      {% for related in relatedFoods %}
      <li>
        <a href="/how-long-does-{{ related.slug }}-last/">
          How long does {{ related.name }} last?
        </a>
      </li>
      {% endfor %}
    </ul>
    {% else %}
    <p>No related foods yet.</p>
    {% endif %}
  </section>
  {% endset %}
  {% include "components/ui-card.njk" %}

  {% if adsEnabled %}
  {% set adSlot = {
    enabled: true,
    zone: "bottom_page"
  } %}
  {% include "components/ad-slot.njk" %}
  {% endif %}
</article>
