<h1>{{ prefill.h1 }}</h1>
<p>
  {{ prefill.explanation.summary }}
</p>

<p>
  Related references:
  <a href="/how-long-does-{{ prefill.food_slug }}-last/">food storage page</a>,
  <a href="/methodology/">methodology</a>,
  <a href="/disclaimer/">disclaimer</a>.
</p>

{% include "components/sitout-calculator-ui.njk" %}

<section>
  <h2>Why this result</h2>
  <p>
    This output comes from the configured conservative rule hierarchy:
    <strong>state-specific -> food-specific -> category-specific</strong>.
  </p>
  <ul>
    {% for reason in calculatorContext.initialResult.reasons %}
    <li>{{ reason }}</li>
    {% endfor %}
  </ul>
</section>

<section>
  <h2>What changes the outcome</h2>
  <ul>
    {% for item in prefill.explanation.outcome_changes %}
    <li>{{ item }}</li>
    {% endfor %}
  </ul>
</section>
